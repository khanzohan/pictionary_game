# Pictionary Game Frontend Environment Variables
# Copy this file to .env for local development

# Development server configuration (optional)
# VITE_PORT=3000
# VITE_HOST=localhost

# API Configuration (for future backend integration)
# VITE_API_URL=http://localhost:8000
# VITE_WS_URL=ws://localhost:8000/ws

# Game Configuration (optional overrides)
# VITE_ROUND_TIME=60
# VITE_MAX_PLAYERS=8

# Feature Flags (optional)
# VITE_ENABLE_SOUNDS=true
# VITE_ENABLE_CHAT=true
# VITE_ENABLE_ROOMS=true

# Note: All environment variables for Vite must be prefixed with VITE_
# Currently, this frontend application works without any environment variables
# These are provided for potential future enhancements 